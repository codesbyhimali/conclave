# =============================================
# Supabase Configuration
# =============================================
# Get these values from your Supabase project settings:
# https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Your Supabase project URL (e.g., https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://demo.supabase.co

# Your Supabase anon/public key (safe to expose in the browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=demo_key

# Your Supabase service role key (keep secret! never commit the real value)
# Used for server-side operations that bypass Row Level Security
SUPABASE_SERVICE_ROLE_KEY=demo_service_key

# =============================================
# Optional Configuration
# =============================================

# Secret token for the cleanup cron job endpoint
# Generate a secure random string: openssl rand -base64 32
# This protects your /api/cleanup endpoint from unauthorized access
CLEANUP_SECRET_TOKEN=demo_cleanup_token

# =============================================
# Setup Instructions
# =============================================
# 1. Create a Supabase project at https://supabase.com
# 2. Run the SQL schema from src/lib/db/schema.sql in your SQL Editor
# 3. Create a storage bucket named "temp-uploads" in Storage settings
#    - Make it public or configure appropriate policies
# 4. Configure Authentication:
#    - Go to Authentication > URL Configuration
#    - Add your site URL (e.g., http://localhost:3000 for development)
#    - Add redirect URLs: http://localhost:3000/auth/callback
# 5. Configure Email Provider (Authentication > Providers > Email):
#    - Enable email provider
#    - For development: Disable "Confirm email" to avoid verification step
#    - For production: Configure your SMTP settings or use Supabase's email service
# 6. Copy this file to .env.local and fill in your actual values
# 7. Never commit .env.local to version control!

